target/
**/node_modules/
**/dist/
.DS_Store
.env
.references/

# FUSE leftovers from editing over a mounted filesystem
.fuse_hidden*
.vercel
.env*
# ...but the template must ship: the README tells people to copy it, and it
# holds public testnet addresses only.
!.env.example
!**/.env.example
